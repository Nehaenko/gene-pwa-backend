/* eslint-disable */
define([
    'uiComponent'

], function (Component) {
    'use strict';

    return Component.extend({
        defaults: {
            template: 'Gene_Framework/shipping-form-title'
        }
    });
});
